# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Environment variables — never commit secrets
.env

# Data — raw CSV stays local, not in Git
data/raw/
data/processed/

# Model artifacts — can be large, regenerated by train.py
models/artifacts/

# OS
.DS_Store
Thumbs.db

# Local Claude Code context — never pushed
.claudecontext/
